<% content_for :jumbotron do %>
  <%= render partial: 'shared/jumbotron', locals: {
    titles: {
      title: @gallery.name,
      subtitle: @gallery.author
    },
    add_link_name: 'Add New Picture',
    add_link_path: new_gallery_picture_path
  } %>
<% end %>

<%= render partial: 'shared/picture', collection: @pictures %>